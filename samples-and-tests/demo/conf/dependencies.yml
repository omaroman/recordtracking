# Application dependencies

require:
    - play
    - play -> crud
    - play -> secure
    - play -> elasticsearch 0.3

    - recordtracking -> recordtracking

repositories:
    - test recordtracking module:
        type:       local
        artifact:   ${application.path}/../../../[module]
        contains:
            - recordtracking -> *
